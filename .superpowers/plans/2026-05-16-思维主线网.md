---

authored_by: external
content_role: reference
trust_level: primary
authoring_mode: human
last_verified: 2026-05-17
---
# 单期思维主线网 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在颖响力网页 EP001 详情页新增"思维主线网"视图——横向主线展示讲者 8 段讲述顺序，每段向外辐射分支节点（概念/模型/人物/跨期/边界），点段节点切换焦点模式查看字幕引用与全部延展。

**Architecture:** 纯 SVG + DOM 模板字符串渲染（与现有 `src/app.js` 模式一致），无新依赖。数据驱动：扩展 `EP001.json` 加 `segments[]` 字段，每段引用现有 `concepts/models/people/relatedEpisodes/boundaries`。新增独立模块 `src/episode-mainline.js`，由 `renderEpisodeDetail()` 在 viewpoints 区块前调用。样式遵循 Linear/Vercel 现代极简深色风格，新增独立 `.mainline-*` 命名空间避免污染。

**Tech Stack:** Vanilla ES Modules JS、SVG、CSS（项目无打包，`build.mjs` 直接 copy `src/` 到 `dist/assets` + `docs/assets`）；浏览器视觉验证（项目无单元测试框架，仅 `scripts/ui-smoke.mjs` 端到端冒烟）。

**Testing approach note:** 该项目无 jest/vitest，验证方式为：(1) `npm run build` 通过、(2) `npm run dev` + 打开 EP001 页面视觉验证、(3) `npm run test:ui` 端到端冒烟不破坏。每个 Task 末尾都包含这三类验证 step。

**Spec reference:** `.superpowers/specs/2026-05-16-思维主线网-design.md`

---

## File Structure

| 文件 | 类型 | 责任 |
|---|---|---|
| `content/episodes/EP001.json` | Modify | 新增顶层 `segments[]` 字段（8 段） |
| `src/episode-mainline.js` | Create | 主线网渲染组件（公开 `renderEpisodeMainline(episode)` 返回 HTML 字符串 + 安装事件 hook） |
| `src/assets/style.css` 或项目实际 style.css | Modify | 追加 `.mainline-*` 样式块（深色、紫主调、克制极简） |
| `src/app.js` | Modify | 在 `renderEpisodeDetail()` 中调用 `renderEpisodeMainline` 并挂载交互 |
| `build.mjs` | Modify | 把 `src/episode-mainline.js` 复制到 `dist/assets` 和 `docs/assets` |

**Working dir 约定**：所有 path 相对项目根 `/Users/ziqiguo/Documents/Diary/GoldenVault/md/topic/personal/sources/people/颖响力/网页/`。任务中标注的绝对路径都基于此根。

---

## Task 1: 在 EP001.json 补充 segments[] 数据

**Files:**
- Modify: `content/episodes/EP001.json`（顶层加 `segments` 字段，置于 `viewpoints` 之后）

- [ ] **Step 1.1: 读现有 EP001.json 末尾结构**

Run: `python3 -c "import json; d=json.load(open('content/episodes/EP001.json')); print(list(d.keys()))"`

Expected: `['id', 'title', 'publishedAt', 'summary', 'tags', 'people', 'themes', 'concepts', 'models', 'relatedEpisodes', 'topic', 'viewpoints', 'extensions', 'videoLinks']`

- [ ] **Step 1.2: 在 viewpoints 字段之后新增 segments 数组**

打开 `content/episodes/EP001.json`，在 `"extensions": [...]` 之前插入以下顶层字段（保持 JSON 合法）：

```json
"segments": [
  {
    "id": "s1",
    "title": "开场设问",
    "blurb": "凭什么这么贵",
    "startTs": "00:00:00",
    "endTs": "00:00:11",
    "keyQuote": "这西贝啊他为什么敢卖这么贵，我也苦苦寻觅了好几天……罗永浩终于让我找到了答案",
    "isCore": false,
    "branches": {
      "concepts": [],
      "models": [],
      "people": ["罗永浩"],
      "relatedEpisodes": [],
      "boundaries": [],
      "annotations": []
    }
  },
  {
    "id": "s2",
    "title": "咨询费",
    "blurb": "6000万 / 10年",
    "startTs": "00:00:11",
    "endTs": "00:00:47",
    "keyQuote": "这10年就收了西贝6000万的一个咨询费……他怎么能负担起这么高的咨询费用，那势必它的餐饮价格不能低了",
    "isCore": false,
    "branches": {
      "concepts": ["cost-shifting"],
      "models": ["principal-agent"],
      "people": [],
      "relatedEpisodes": ["EP002"],
      "boundaries": [],
      "annotations": []
    }
  },
  {
    "id": "s3",
    "title": "超级符号",
    "blurb": "红白格 low",
    "startTs": "00:00:47",
    "endTs": "00:01:37",
    "keyQuote": "华与华最擅长的就是做超级符号……那个红白格子属实是不太高级……那大概就相当于我们那社区食堂的装修标准",
    "isCore": false,
    "branches": {
      "concepts": ["symbol-mismatch"],
      "models": [],
      "people": [],
      "relatedEpisodes": ["EP004"],
      "boundaries": ["未谈：食材与租金等真实成本结构"],
      "annotations": []
    }
  },
  {
    "id": "s4",
    "title": "帝王思维",
    "blurb": "巡店没人敢说",
    "startTs": "00:01:37",
    "endTs": "00:02:38",
    "keyQuote": "贾老板这个人……有点中国第一代企业都有的，就是有点帝王思维……所有人都是等着贾老板发言",
    "isCore": false,
    "branches": {
      "concepts": ["organizational-silence"],
      "models": ["feedback-loops"],
      "people": ["贾国龙"],
      "relatedEpisodes": [],
      "boundaries": [],
      "annotations": []
    }
  },
  {
    "id": "s5",
    "title": "皇帝的新衣",
    "blurb": "讲者的核心隐喻 · 串起前 4 段",
    "startTs": "00:02:38",
    "endTs": "00:03:50",
    "keyQuote": "我觉得华与华就扮演了这么一个角色，他深知贾老板喜欢的是什么，他也深知贾老板这个企业欠缺什么，可是他不说呀，他为了挣钱",
    "isCore": true,
    "branches": {
      "concepts": ["organizational-silence", "appeasing-consultant", "symbol-mismatch"],
      "models": ["authority-misinfluence", "principal-agent"],
      "people": ["华与华", "贾国龙"],
      "relatedEpisodes": ["EP065"],
      "boundaries": ["童话里的'小孩'现实中是谁——讲者把'沉默'画成铁律，但罗永浩就是这次的小孩", "把华与华定义为'骗子'是修辞强势——也许他们真的相信自己的方法论"],
      "annotations": []
    }
  },
  {
    "id": "s6",
    "title": "商业模式",
    "blurb": "机场广告给帝王看",
    "startTs": "00:03:50",
    "endTs": "00:04:30",
    "keyQuote": "你看到每个机场里边都能看到那个华与华的广告，这兄弟俩往那一站那种状态，其实就是告诉那些老板……你们需要就过来找我",
    "isCore": false,
    "branches": {
      "concepts": ["appeasing-consultant"],
      "models": [],
      "people": ["华与华"],
      "relatedEpisodes": [],
      "boundaries": [],
      "annotations": []
    }
  },
  {
    "id": "s7",
    "title": "总结因果",
    "blurb": "成本→消费者",
    "startTs": "00:04:30",
    "endTs": "00:05:21",
    "keyQuote": "这些老板们到一定水平上，真正成了一个快成为帝王了……就自己的脑子就开始不清楚了……谁懂他们给他们想要的，谁就能达到丰厚的回报",
    "isCore": false,
    "branches": {
      "concepts": ["premium-narrative"],
      "models": ["value-capture"],
      "people": [],
      "relatedEpisodes": [],
      "boundaries": ["未谈：消费者为何愿为高价 / 帝王叙事买单（→ EP071 心理机制）"],
      "annotations": []
    }
  },
  {
    "id": "s8",
    "title": "反问收尾",
    "blurb": "你们自己想",
    "startTs": "00:05:21",
    "endTs": "00:05:31",
    "keyQuote": "那你说西贝是这样，是不是还有别的地方是这样……像华与华这样的人呢，那我就不好说了，你们自己想",
    "isCore": false,
    "branches": {
      "concepts": [],
      "models": [],
      "people": [],
      "relatedEpisodes": ["EP065", "EP071"],
      "boundaries": [],
      "annotations": []
    }
  }
],
```

- [ ] **Step 1.3: 验证 JSON 合法**

Run: `python3 -c "import json; json.load(open('content/episodes/EP001.json'))"`

Expected: 无输出（JSON 合法）。若报错，根据行号修复缺少的逗号或括号。

- [ ] **Step 1.4: 验证 build 通过**

Run: `npm run build`

Expected: 无报错；`dist/data/site.json` 中 EP001 应包含 `segments`。

Verify: `python3 -c "import json; d=json.load(open('dist/data/site.json')); ep=[e for e in d['episodes'] if e['id']=='EP001'][0]; print(len(ep.get('segments',[])), '段')"`
Expected: `8 段`

- [ ] **Step 1.5: 提交**

```bash
git add content/episodes/EP001.json
git commit -m "data(EP001): 新增 segments[] 字段供思维主线网消费"
```

---

## Task 2: 新建 episode-mainline.js 模块骨架（仅主线 + 段节点，不带分支）

**Files:**
- Create: `src/episode-mainline.js`
- Modify: `build.mjs:954-956` 和 `build.mjs:961-964`（在 copyFile 列表里加 `episode-mainline.js`）
- Modify: `src/app.js:6944-7018` 范围内（找到 `renderEpisodeKnowledgeMap(episode)` 那一行附近，在它之前插入挂载点）

- [ ] **Step 2.1: 创建 src/episode-mainline.js 骨架**

Create file `src/episode-mainline.js`:

```javascript
// 单期思维主线网渲染组件
// 公开两个函数：
//   renderEpisodeMainline(episode) → HTML 字符串（嵌入到 renderEpisodeDetail 模板）
//   attachEpisodeMainlineHandlers(rootEl) → 在该 DOM 子树中绑定交互事件

const MAINLINE_WIDTH = 1180;
const MAINLINE_HEIGHT = 460;
const PADDING_X = 80;
const MAIN_Y = MAINLINE_HEIGHT / 2;

function escapeHtml(value) {
  return String(value ?? '')
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#39;');
}

function segmentCenter(index, total) {
  if (total <= 1) return MAINLINE_WIDTH / 2;
  const usable = MAINLINE_WIDTH - PADDING_X * 2;
  return PADDING_X + (usable / (total - 1)) * index;
}

export function renderEpisodeMainline(episode) {
  const segments = Array.isArray(episode?.segments) ? episode.segments : [];
  if (segments.length === 0) return '';

  const total = segments.length;

  const segNodes = segments.map((seg, idx) => {
    const cx = segmentCenter(idx, total);
    const cy = MAIN_Y;
    const isCore = !!seg.isCore;
    const radius = isCore ? 22 : 14;
    const cls = `mainline-seg-node${isCore ? ' is-core' : ''}`;
    return `
      <g class="${cls}" data-segment-id="${escapeHtml(seg.id)}" transform="translate(${cx},${cy})">
        <circle r="${radius}" class="mainline-seg-circle"></circle>
        <text class="mainline-seg-index" text-anchor="middle" dy="0.32em">${isCore ? '★' : String(idx + 1)}</text>
        <text class="mainline-seg-title" text-anchor="middle" y="${radius + 18}">${escapeHtml(seg.title)}</text>
        <text class="mainline-seg-blurb" text-anchor="middle" y="${radius + 32}">${escapeHtml(seg.blurb || '')}</text>
      </g>
    `;
  }).join('');

  return `
    <section class="detail-section mainline-section">
      <h2>思维主线</h2>
      <p class="mainline-subtitle">讲者讲述的 ${total} 段逻辑路线 · 点段节点查看字幕和延展</p>
      <div class="mainline-canvas">
        <svg class="mainline-svg" viewBox="0 0 ${MAINLINE_WIDTH} ${MAINLINE_HEIGHT}" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="EP001 思维主线">
          <line class="mainline-axis" x1="${PADDING_X}" y1="${MAIN_Y}" x2="${MAINLINE_WIDTH - PADDING_X}" y2="${MAIN_Y}"></line>
          ${segNodes}
        </svg>
      </div>
    </section>
  `;
}

export function attachEpisodeMainlineHandlers(rootEl) {
  if (!rootEl) return;
  const nodes = rootEl.querySelectorAll('.mainline-seg-node');
  nodes.forEach((node) => {
    node.addEventListener('click', () => {
      const id = node.getAttribute('data-segment-id');
      // TODO Task 4: 切换焦点模式 + 打开 panel
      console.log('[mainline] segment clicked', id);
    });
  });
}
```

- [ ] **Step 2.2: 在 build.mjs 里 copy 新文件**

修改 `build.mjs` 两段：

第一处：约 `build.mjs:954-957`（dist 复制段），在 `copyFile(path.join(srcDir, 'graph-view.js'), path.join(assetsDir, 'graph-view.js')),` 之后加一行：

```javascript
    copyFile(path.join(srcDir, 'episode-mainline.js'), path.join(assetsDir, 'episode-mainline.js')),
```

第二处：约 `build.mjs:961-964`（docs 复制段），在 `copyFile(path.join(srcDir, 'graph-view.js'), path.join(docsAssetsDir, 'graph-view.js')),` 之后加一行：

```javascript
    copyFile(path.join(srcDir, 'episode-mainline.js'), path.join(docsAssetsDir, 'episode-mainline.js')),
```

- [ ] **Step 2.3: 在 src/app.js 顶部加 import**

在 `src/app.js` 文件最顶部（在第 1 行 `const floatingEpisodeSearchButton = ...` 之前）加：

```javascript
import { renderEpisodeMainline, attachEpisodeMainlineHandlers } from './episode-mainline.js';
```

**注意**: 检查 `src/app.js` 是否本来就是 ES Module（`<script type="module">` 加载）。查看 `src/index.html` 第 78 行附近 `<script type="module" src="./assets/app.js"></script>` —— 是的，已经是 module。可以直接 import。

- [ ] **Step 2.4: 在 renderEpisodeDetail 中挂载主线网**

找到 `src/app.js:6987-7019`（`renderEpisodeDetail` 中 `app.innerHTML = ...` 模板）。在 `${renderEpisodeKnowledgeMap(episode)}` 这一行之前（约 7018 行）插入：

```javascript
      ${renderEpisodeMainline(episode)}
```

然后在 `app.innerHTML = ...` 整个赋值语句之后（约 7080-7090 范围，`</section>`; 后），加入挂载事件 hook：

```javascript
  attachEpisodeMainlineHandlers(app);
```

找到合适位置：搜索 `renderEpisodeDetail(id)` 函数末尾的最后一个有意义语句（在 `app.innerHTML = ...` 模板之后，函数 `}` 闭合之前）。

- [ ] **Step 2.5: 加最简样式（保证可见，不追求精致）**

修改 `src/style.css`，在文件末尾追加：

```css
/* === 思维主线网（Task 2 骨架）=== */
.mainline-section { background: #0d0d0e; color: #e5e5e5; padding: 24px 20px; border-radius: 12px; margin: 24px 0; }
.mainline-section h2 { color: #f5f5f5; margin: 0 0 4px; font-size: 18px; font-weight: 500; }
.mainline-subtitle { color: #888; font-size: 12.5px; margin: 0 0 16px; }
.mainline-canvas { width: 100%; overflow-x: auto; }
.mainline-svg { display: block; width: 100%; height: auto; max-height: 460px; }
.mainline-axis { stroke: rgba(167,139,250,0.35); stroke-width: 2; }
.mainline-seg-node { cursor: pointer; }
.mainline-seg-circle { fill: #8b5cf6; stroke: #c4b5fd; stroke-width: 1.5; transition: transform 200ms ease; transform-origin: center; transform-box: fill-box; }
.mainline-seg-node:hover .mainline-seg-circle { transform: scale(1.15); }
.mainline-seg-node.is-core .mainline-seg-circle { fill: #fbbf24; stroke: #fef3c7; stroke-width: 2; }
.mainline-seg-index { fill: #fff; font-size: 10px; font-weight: 600; }
.mainline-seg-title { fill: #e5e5e5; font-size: 12px; }
.mainline-seg-blurb { fill: #888; font-size: 10px; }
```

- [ ] **Step 2.6: 验证 build 通过**

Run: `npm run build`
Expected: 无错；`dist/assets/episode-mainline.js` 存在。
Verify: `ls -la dist/assets/episode-mainline.js docs/assets/episode-mainline.js`
Expected: 两个文件都存在。

- [ ] **Step 2.7: 浏览器验证（视觉冒烟）**

Run: `npm run dev`（后台启动，或新开终端 `npm run serve`，已 build 后用 `npm run serve` 即可）

打开浏览器 → http://localhost:4310/#/episodes/EP001

预期：在「核心观点」区块下方出现「思维主线」section，深色背景上有一条紫色横线，线上 8 个圆球，从左到右标 1-4、★、6-8，每球下方有段标题（开场设问、咨询费、超级符号、帝王思维、皇帝的新衣、商业模式、总结因果、反问收尾）。第 5 个球是金色 ★。

Hover 任一球应有微放大。点击应在 console 打出 `[mainline] segment clicked s1` 等。

- [ ] **Step 2.8: 跑 UI smoke 不破坏**

Run: `npm run test:ui`
Expected: 通过（项目原有的 ui-smoke 测试）。如有非主线相关失败，分析原因（可能现有就有失败 — 比对 git log 看是否本来就 fail）。

- [ ] **Step 2.9: 提交**

```bash
git add src/episode-mainline.js src/app.js src/style.css build.mjs
git commit -m "feat(mainline): 新增思维主线网骨架（主线 + 8 段节点 + 段 5 金高亮）"
```

---

## Task 3: 加分支节点（默认扫读模式：每段渲染 2 个最关键分支）

**Files:**
- Modify: `src/episode-mainline.js`（renderEpisodeMainline 内增加分支渲染逻辑）
- Modify: `src/style.css`（追加 `.mainline-branch-*` 样式）

- [ ] **Step 3.1: 在 episode-mainline.js 中加分支选择和渲染逻辑**

修改 `src/episode-mainline.js`，在 `function segmentCenter` 之后、`export function renderEpisodeMainline` 之前插入辅助函数：

```javascript
// 分支选择优先级（每段默认显示前 2 个）
const BRANCH_PRIORITY = ['people', 'relatedEpisodes', 'concepts', 'models', 'boundaries', 'annotations'];

const BRANCH_META = {
  concepts: { color: '#0284c7', label: '概念' },
  models: { color: '#16a34a', label: '模型' },
  people: { color: '#ea580c', label: '人物' },
  relatedEpisodes: { color: '#7c3aed', label: '跨期' },
  boundaries: { color: '#9ca3af', label: '边界', dashed: true },
  annotations: { color: '#c79235', label: '批注' }
};

function collectBranches(segment, site) {
  const result = [];
  for (const type of BRANCH_PRIORITY) {
    const ids = segment.branches?.[type] || [];
    for (const id of ids) {
      let label = id;
      if (type === 'concepts') label = site?.concepts?.find?.((c) => c.id === id)?.name || id;
      else if (type === 'models') label = site?.models?.find?.((m) => m.id === id)?.name || id;
      else if (type === 'relatedEpisodes') label = id;  // EP002 etc.
      else if (type === 'boundaries' || type === 'annotations') label = id.length > 18 ? id.slice(0, 18) + '…' : id;
      result.push({ type, id, label });
    }
  }
  return result;
}

function branchPosition(segCx, segCy, branchIdx, totalBranches, isAbove) {
  // 上方 / 下方各分布；垂直距离 100-160；水平 spread 取决于 totalBranches
  const verticalDist = 110 + (branchIdx % 2 === 0 ? 0 : 30);
  const horizontalSpread = totalBranches > 1 ? (branchIdx - (totalBranches - 1) / 2) * 50 : 0;
  return {
    x: segCx + horizontalSpread,
    y: isAbove ? segCy - verticalDist : segCy + verticalDist
  };
}
```

接着，修改 `renderEpisodeMainline(episode)` 函数签名为 `renderEpisodeMainline(episode, site)`（增加 site 参数）。修改 segNodes 渲染部分，让每段输出分支：

替换原有 `segments.map(...)` 整段为：

```javascript
  const SCAN_LIMIT = 2;  // 默认扫读模式：每段最多 2 个分支

  const segElements = segments.map((seg, idx) => {
    const cx = segmentCenter(idx, total);
    const cy = MAIN_Y;
    const isCore = !!seg.isCore;
    const radius = isCore ? 22 : 14;

    const allBranches = collectBranches(seg, site);
    const scanBranches = allBranches.slice(0, SCAN_LIMIT);

    const branchUp = scanBranches.filter((b) => ['concepts', 'models'].includes(b.type));
    const branchDown = scanBranches.filter((b) => !['concepts', 'models'].includes(b.type));

    const renderBranch = (b, i, count, isAbove) => {
      const pos = branchPosition(cx, cy, i, count, isAbove);
      const meta = BRANCH_META[b.type] || {};
      const dashed = meta.dashed ? 'stroke-dasharray="3 2"' : '';
      const lineY1 = isAbove ? cy - radius : cy + radius;
      return `
        <g class="mainline-branch mainline-branch-${b.type}" data-branch-type="${b.type}" data-branch-id="${escapeHtml(b.id)}">
          <line class="mainline-branch-line" x1="${cx}" y1="${lineY1}" x2="${pos.x}" y2="${pos.y}" ${dashed} style="stroke:${meta.color};"></line>
          <circle class="mainline-branch-dot" cx="${pos.x}" cy="${pos.y}" r="5" style="fill:${meta.color};"></circle>
          <text class="mainline-branch-label" x="${pos.x}" y="${pos.y + (isAbove ? -10 : 16)}" text-anchor="middle" style="fill:${meta.color};">${escapeHtml(b.label)}</text>
        </g>
      `;
    };

    const upElems = branchUp.map((b, i) => renderBranch(b, i, branchUp.length, true)).join('');
    const downElems = branchDown.map((b, i) => renderBranch(b, i, branchDown.length, false)).join('');

    return `
      <g class="mainline-seg-node${isCore ? ' is-core' : ''}" data-segment-id="${escapeHtml(seg.id)}" transform="translate(0,0)">
        ${upElems}
        ${downElems}
        <g transform="translate(${cx},${cy})">
          <circle r="${radius}" class="mainline-seg-circle"></circle>
          <text class="mainline-seg-index" text-anchor="middle" dy="0.32em">${isCore ? '★' : String(idx + 1)}</text>
          <text class="mainline-seg-title" text-anchor="middle" y="${radius + 18}">${escapeHtml(seg.title)}</text>
          <text class="mainline-seg-blurb" text-anchor="middle" y="${radius + 32}">${escapeHtml(seg.blurb || '')}</text>
        </g>
      </g>
    `;
  }).join('');
```

将 `${segNodes}` 替换为 `${segElements}`。

- [ ] **Step 3.2: 在 src/app.js 调用点传入 site**

找到 `src/app.js` 中 `${renderEpisodeMainline(episode)}` 那一行（Task 2.4 加入的），改为：

```javascript
      ${renderEpisodeMainline(episode, site)}
```

注意：`site` 在 `src/app.js` 顶部已是全局可用变量（被 `loadSite()` 填充）。

- [ ] **Step 3.3: 加分支样式**

修改 `src/style.css`，在 Task 2 加的 `.mainline-*` 块末尾追加：

```css
.mainline-branch-line { stroke-width: 1; opacity: 0.55; }
.mainline-branch-dot { stroke: rgba(255,255,255,0.15); stroke-width: 1; }
.mainline-branch-label { font-size: 10.5px; pointer-events: none; }
.mainline-branch { cursor: pointer; transition: opacity 200ms ease; }
.mainline-branch:hover .mainline-branch-dot { stroke: #fff; stroke-width: 1.5; }
```

- [ ] **Step 3.4: 验证 build**

Run: `npm run build`
Expected: 无错。

- [ ] **Step 3.5: 浏览器验证（扫读模式）**

刷新浏览器 → EP001 页面。

预期：每个段节点周围出现至多 2 个小彩色圆点（按类型颜色），连线浅色；段 2 应有 EP002 跨期点（紫）；段 4 有「贾国龙」（橙）；段 5 ★ 周围有 2 个最优先级分支；段 8 有 EP065/EP071。

视觉上不应"乱"——每段最多 2 个分支，呼吸感保留。

- [ ] **Step 3.6: 跑 UI smoke**

Run: `npm run test:ui`
Expected: 通过。

- [ ] **Step 3.7: 提交**

```bash
git add src/episode-mainline.js src/app.js src/style.css
git commit -m "feat(mainline): 加分支节点（扫读模式：每段 2 个最关键分支）"
```

---

## Task 4: 焦点模式 + 右侧 panel

**Files:**
- Modify: `src/episode-mainline.js`（增加 panel HTML、focus 状态、点击切换）
- Modify: `src/style.css`（追加 panel 样式 + .focused 状态样式）

- [ ] **Step 4.1: 重新设计 episode-mainline.js 输出**

修改 `src/episode-mainline.js`，让 `renderEpisodeMainline` 同时输出 panel 容器（默认隐藏）。在 SVG `</svg>` 之后、`</div></section>` 之前插入：

```html
<aside class="mainline-panel" data-mainline-panel hidden>
  <button class="mainline-panel-close" type="button" aria-label="关闭">×</button>
  <div class="mainline-panel-body"></div>
</aside>
```

确切位置：在 `renderEpisodeMainline` 函数中，把 `return` 模板字符串改为：

```javascript
  return `
    <section class="detail-section mainline-section">
      <h2>思维主线</h2>
      <p class="mainline-subtitle">讲者讲述的 ${total} 段逻辑路线 · 点段节点查看字幕和延展</p>
      <div class="mainline-canvas" data-mainline-canvas>
        <svg class="mainline-svg" viewBox="0 0 ${MAINLINE_WIDTH} ${MAINLINE_HEIGHT}" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="EP001 思维主线">
          <line class="mainline-axis" x1="${PADDING_X}" y1="${MAIN_Y}" x2="${MAINLINE_WIDTH - PADDING_X}" y2="${MAIN_Y}"></line>
          ${segElements}
        </svg>
        <aside class="mainline-panel" data-mainline-panel hidden>
          <button class="mainline-panel-close" type="button" aria-label="关闭">×</button>
          <div class="mainline-panel-body"></div>
        </aside>
      </div>
    </section>
  `;
```

- [ ] **Step 4.2: 在 episode-mainline.js 中加 panel 渲染辅助 + 焦点切换逻辑**

在文件末尾（在 `export function attachEpisodeMainlineHandlers` 之前）加 panel HTML 生成函数：

```javascript
function renderPanelHtml(segment, site) {
  const allBranches = collectBranches(segment, site);
  const byType = {};
  for (const b of allBranches) {
    if (!byType[b.type]) byType[b.type] = [];
    byType[b.type].push(b);
  }

  const groupHtml = (type, items) => {
    if (!items || items.length === 0) return '';
    const meta = BRANCH_META[type];
    return `
      <div class="mainline-panel-group">
        <h5 style="color:${meta.color};">${escapeHtml(meta.label)}（${items.length}）</h5>
        <ul>
          ${items.map((it) => `<li><span class="mainline-panel-dot" style="background:${meta.color};"></span>${escapeHtml(it.label)}</li>`).join('')}
        </ul>
      </div>
    `;
  };

  return `
    <div class="mainline-panel-header">
      <span class="mainline-panel-index">${segment.isCore ? '★ ' : ''}段 ${escapeHtml(String(segment.id).replace('s', ''))}</span>
      <h4>${escapeHtml(segment.title)}</h4>
      <p class="mainline-panel-blurb">${escapeHtml(segment.blurb || '')}</p>
      <p class="mainline-panel-ts">${escapeHtml(segment.startTs || '')} – ${escapeHtml(segment.endTs || '')}</p>
    </div>
    ${segment.keyQuote ? `<blockquote class="mainline-panel-quote">${escapeHtml(segment.keyQuote)}</blockquote>` : ''}
    <div class="mainline-panel-branches">
      ${BRANCH_PRIORITY.map((type) => groupHtml(type, byType[type])).join('')}
    </div>
  `;
}
```

- [ ] **Step 4.3: 改写 attachEpisodeMainlineHandlers 实现焦点切换**

替换 `attachEpisodeMainlineHandlers` 整个函数：

```javascript
export function attachEpisodeMainlineHandlers(rootEl, episode, site) {
  if (!rootEl) return;
  const canvas = rootEl.querySelector('[data-mainline-canvas]');
  if (!canvas) return;
  const panel = canvas.querySelector('[data-mainline-panel]');
  const panelBody = panel?.querySelector('.mainline-panel-body');
  const closeBtn = panel?.querySelector('.mainline-panel-close');
  const segNodes = canvas.querySelectorAll('.mainline-seg-node');

  function openFocus(segmentId) {
    const seg = (episode?.segments || []).find((s) => s.id === segmentId);
    if (!seg || !panelBody || !panel) return;
    canvas.classList.add('mainline-focus-on');
    segNodes.forEach((n) => {
      if (n.getAttribute('data-segment-id') === segmentId) {
        n.classList.add('is-focused');
        n.classList.remove('is-dimmed');
      } else {
        n.classList.remove('is-focused');
        n.classList.add('is-dimmed');
      }
    });
    panelBody.innerHTML = renderPanelHtml(seg, site);
    panel.hidden = false;
  }

  function closeFocus() {
    canvas.classList.remove('mainline-focus-on');
    segNodes.forEach((n) => {
      n.classList.remove('is-focused');
      n.classList.remove('is-dimmed');
    });
    if (panel) panel.hidden = true;
  }

  segNodes.forEach((node) => {
    node.addEventListener('click', (e) => {
      e.stopPropagation();
      const id = node.getAttribute('data-segment-id');
      openFocus(id);
    });
  });

  closeBtn?.addEventListener('click', closeFocus);
  canvas.addEventListener('click', (e) => {
    if (e.target === canvas || e.target.closest('.mainline-svg') === canvas.querySelector('.mainline-svg') && !e.target.closest('.mainline-seg-node')) {
      closeFocus();
    }
  });
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeFocus();
  });
}
```

- [ ] **Step 4.4: 修改 src/app.js 中 attachEpisodeMainlineHandlers 的调用**

找到 Task 2.4 加入的 `attachEpisodeMainlineHandlers(app);`，改为：

```javascript
  attachEpisodeMainlineHandlers(app, episode, site);
```

- [ ] **Step 4.5: 加焦点模式 + panel 样式**

修改 `src/style.css`，追加：

```css
.mainline-canvas { position: relative; }
.mainline-focus-on .mainline-seg-node.is-dimmed { opacity: 0.25; transition: opacity 250ms ease; }
.mainline-focus-on .mainline-seg-node.is-dimmed .mainline-branch { opacity: 0.15; }
.mainline-focus-on .mainline-seg-node.is-focused .mainline-seg-circle { stroke-width: 2.5; filter: drop-shadow(0 0 8px rgba(196,181,253,0.6)); }
.mainline-focus-on .mainline-seg-node.is-focused.is-core .mainline-seg-circle { filter: drop-shadow(0 0 12px rgba(251,191,36,0.7)); }

.mainline-panel {
  position: absolute;
  top: 0; right: 0;
  width: 360px; max-width: 92vw;
  height: 100%;
  background: #141416;
  border-left: 1px solid rgba(255,255,255,0.08);
  padding: 20px 22px;
  overflow-y: auto;
  box-shadow: -8px 0 24px rgba(0,0,0,0.4);
  color: #e5e5e5;
  z-index: 2;
}
.mainline-panel[hidden] { display: none; }
.mainline-panel-close {
  position: absolute; top: 12px; right: 14px;
  background: transparent; border: none; color: #aaa;
  font-size: 22px; cursor: pointer; padding: 0; line-height: 1;
}
.mainline-panel-close:hover { color: #fff; }
.mainline-panel-header { margin-bottom: 14px; }
.mainline-panel-index { display: inline-block; padding: 3px 8px; background: rgba(196,181,253,0.15); color: #c4b5fd; font-size: 11px; border-radius: 4px; letter-spacing: 0.4px; }
.mainline-panel-header h4 { margin: 8px 0 4px; font-size: 18px; font-weight: 600; color: #f5f5f5; }
.mainline-panel-blurb { margin: 0; font-size: 12.5px; color: #999; }
.mainline-panel-ts { margin: 4px 0 0; font-size: 11px; color: #666; font-variant-numeric: tabular-nums; }
.mainline-panel-quote {
  margin: 14px 0; padding: 12px 14px;
  background: rgba(255,255,255,0.025);
  border-left: 2px solid #c4b5fd;
  border-radius: 0 5px 5px 0;
  font-size: 13px; line-height: 1.65; color: #d8d8d8; font-style: italic;
}
.mainline-panel-branches { display: flex; flex-direction: column; gap: 12px; }
.mainline-panel-group h5 { margin: 0 0 6px; font-size: 10.5px; text-transform: uppercase; letter-spacing: 0.6px; font-weight: 500; }
.mainline-panel-group ul { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 4px; }
.mainline-panel-group li { display: flex; align-items: center; gap: 8px; font-size: 12.5px; padding: 6px 8px; background: rgba(255,255,255,0.02); border-radius: 4px; }
.mainline-panel-dot { display: inline-block; width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
```

- [ ] **Step 4.6: 验证 build**

Run: `npm run build`
Expected: 无错。

- [ ] **Step 4.7: 浏览器验证**

刷新 EP001 页面，点击段 5 ★：
- ✅ 右侧滑出 panel
- ✅ panel 展示 `★ 段 5 / 皇帝的新衣 / 讲者的核心隐喻...`
- ✅ keyQuote 引用块显示「我觉得华与华就扮演了这么一个角色...」
- ✅ 6 类分支按颜色分组显示
- ✅ 其他 7 个段节点变灰
- ✅ 点 × 按钮 / 点空白处 / 按 Esc 都能关闭 panel，其他段恢复

- [ ] **Step 4.8: 跑 UI smoke**

Run: `npm run test:ui`
Expected: 通过。

- [ ] **Step 4.9: 提交**

```bash
git add src/episode-mainline.js src/app.js src/style.css
git commit -m "feat(mainline): 焦点模式 + 右侧 panel（字幕 keyQuote + 全部分支分组）"
```

---

## Task 5: 顶部类型筛选（6 类 toggle）

**Files:**
- Modify: `src/episode-mainline.js`（在 section 顶部增加 toolbar，绑定 toggle 行为）
- Modify: `src/style.css`（追加 toolbar 样式）

- [ ] **Step 5.1: 在 episode-mainline.js 加 toolbar HTML**

修改 `renderEpisodeMainline`，在 `<p class="mainline-subtitle">...</p>` 之后、`<div class="mainline-canvas"...>` 之前插入 toolbar：

```javascript
      <div class="mainline-toolbar" data-mainline-toolbar>
        ${BRANCH_PRIORITY.map((type) => {
          const meta = BRANCH_META[type];
          return `
            <label class="mainline-toolbar-toggle" data-toggle-type="${type}">
              <input type="checkbox" checked />
              <span class="mainline-toolbar-dot" style="background:${meta.color};"></span>
              <span class="mainline-toolbar-label">${escapeHtml(meta.label)}</span>
            </label>
          `;
        }).join('')}
      </div>
```

- [ ] **Step 5.2: 在 attachEpisodeMainlineHandlers 中绑定 toggle 行为**

在 `attachEpisodeMainlineHandlers` 函数内（在 `segNodes.forEach((node) => { ... })` 之前）加：

```javascript
  const toolbar = rootEl.querySelector('[data-mainline-toolbar]');
  if (toolbar) {
    const toggles = toolbar.querySelectorAll('input[type="checkbox"]');
    toggles.forEach((input) => {
      input.addEventListener('change', () => {
        const type = input.closest('[data-toggle-type]')?.getAttribute('data-toggle-type');
        if (!type) return;
        canvas.classList.toggle(`mainline-hide-${type}`, !input.checked);
      });
    });
  }
```

- [ ] **Step 5.3: 加 toolbar + 隐藏类样式**

修改 `src/style.css`，追加：

```css
.mainline-toolbar {
  display: flex; flex-wrap: wrap; gap: 8px;
  margin: 0 0 14px;
  padding: 0 0 12px;
  border-bottom: 1px solid rgba(255,255,255,0.06);
}
.mainline-toolbar-toggle {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 6px 10px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 16px;
  cursor: pointer; user-select: none;
  font-size: 11.5px; color: #bbb;
  transition: background 150ms ease;
}
.mainline-toolbar-toggle:has(input:checked) { background: rgba(255,255,255,0.06); color: #e5e5e5; }
.mainline-toolbar-toggle input { display: none; }
.mainline-toolbar-dot { width: 9px; height: 9px; border-radius: 50%; }

/* 隐藏类型：分支节点 fade out */
.mainline-hide-concepts .mainline-branch-concepts,
.mainline-hide-models .mainline-branch-models,
.mainline-hide-people .mainline-branch-people,
.mainline-hide-relatedEpisodes .mainline-branch-relatedEpisodes,
.mainline-hide-boundaries .mainline-branch-boundaries,
.mainline-hide-annotations .mainline-branch-annotations {
  opacity: 0.08; pointer-events: none; transition: opacity 200ms ease;
}
```

- [ ] **Step 5.4: 验证 build**

Run: `npm run build`
Expected: 无错。

- [ ] **Step 5.5: 浏览器验证**

刷新 EP001。预期：思维主线 section 顶部出现 6 个圆角 toggle（概念/模型/人物/跨期/边界/批注），默认都勾选。点击「人物」取消勾选，所有橙色分支节点几乎消失（opacity 8%）。再勾回去恢复。

- [ ] **Step 5.6: 跑 UI smoke**

Run: `npm run test:ui`
Expected: 通过。

- [ ] **Step 5.7: 提交**

```bash
git add src/episode-mainline.js src/style.css
git commit -m "feat(mainline): 顶部 6 类分支 toggle 筛选"
```

---

## Task 6: 动画与打磨（hover 缩放、focus 过渡、节点 stagger 出场）

**Files:**
- Modify: `src/style.css`（增强动画 transition / 增加 stagger 关键帧）

- [ ] **Step 6.1: 在 style.css 加入场动画**

修改 `src/style.css`，追加：

```css
/* === 思维主线网 · 入场与动效 === */
@keyframes mainline-fadein {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: translateY(0); }
}
.mainline-section { animation: mainline-fadein 350ms ease-out both; }

@keyframes mainline-branch-pop {
  from { opacity: 0; transform: scale(0.6); transform-origin: center; transform-box: fill-box; }
  to { opacity: 1; transform: scale(1); }
}
.mainline-branch { animation: mainline-branch-pop 280ms ease-out both; }
.mainline-seg-node:nth-of-type(1) .mainline-branch { animation-delay: 80ms; }
.mainline-seg-node:nth-of-type(2) .mainline-branch { animation-delay: 130ms; }
.mainline-seg-node:nth-of-type(3) .mainline-branch { animation-delay: 180ms; }
.mainline-seg-node:nth-of-type(4) .mainline-branch { animation-delay: 230ms; }
.mainline-seg-node:nth-of-type(5) .mainline-branch { animation-delay: 280ms; }
.mainline-seg-node:nth-of-type(6) .mainline-branch { animation-delay: 330ms; }
.mainline-seg-node:nth-of-type(7) .mainline-branch { animation-delay: 380ms; }
.mainline-seg-node:nth-of-type(8) .mainline-branch { animation-delay: 430ms; }

/* panel 滑入 */
@keyframes mainline-panel-in {
  from { opacity: 0; transform: translateX(20px); }
  to { opacity: 1; transform: translateX(0); }
}
.mainline-panel:not([hidden]) { animation: mainline-panel-in 280ms ease-out both; }

/* hover 增强 */
.mainline-branch:hover .mainline-branch-label { font-weight: 600; }
.mainline-seg-node .mainline-seg-circle { transition: transform 200ms ease, filter 250ms ease; }

/* reduced motion 尊重 */
@media (prefers-reduced-motion: reduce) {
  .mainline-section,
  .mainline-branch,
  .mainline-panel:not([hidden]) { animation: none; }
  .mainline-seg-node .mainline-seg-circle { transition: none; }
}
```

- [ ] **Step 6.2: 验证 build**

Run: `npm run build`
Expected: 无错。

- [ ] **Step 6.3: 浏览器验证**

硬刷新（Cmd+Shift+R）EP001 页面：
- ✅ 整个 section 淡入
- ✅ 分支节点 stagger 依次弹出
- ✅ Hover 段节点 → 圆球微放大；Hover 分支 → 标签变粗
- ✅ 点段 5 → 其他段平滑灰化、panel 从右滑入

- [ ] **Step 6.4: 跑 UI smoke**

Run: `npm run test:ui`
Expected: 通过。

- [ ] **Step 6.5: 提交**

```bash
git add src/style.css
git commit -m "polish(mainline): 入场淡入、分支 stagger、panel 滑入、hover 微动效"
```

---

## Task 7: 整体视觉打磨与最终验收

**Files:**
- Modify (按需): `src/style.css`、`src/episode-mainline.js`（视用户反馈调整字号、间距、配色细节）

- [ ] **Step 7.1: 浏览器走完整 happy-path 一遍**

打开 EP001：
1. ✅ 看到「思维主线」section（在「核心观点」之后、「关联概念/模型」之前）
2. ✅ 默认扫读：8 段节点，每段最多 2 个分支，段 5 ★ 金色
3. ✅ Hover 段 / 分支有微动效
4. ✅ 点段 5 → 焦点模式（其他段灰化）+ 右侧 panel
5. ✅ Panel 显示 ★ 标签、标题、blurb、时间戳、keyQuote 引用、6 类分支分组
6. ✅ Esc / × / 点空白处关闭 panel
7. ✅ 顶部 toggle 6 类筛选生效
8. ✅ 整页其他区块（话题/核心观点/关联概念/延展）未被破坏

- [ ] **Step 7.2: 走其他 episode 不破坏**

打开 EP002 / EP004 / EP065 等没有 segments[] 字段的 episode 详情页：
- ✅ 不应出现「思维主线」section（renderEpisodeMainline 在 segments 为空时返回空串）
- ✅ 其他区块正常显示

- [ ] **Step 7.3: 最后跑 UI smoke 完整通过**

Run: `npm run test:ui`
Expected: 完整通过。

- [ ] **Step 7.4: 检查 git status，确认无意外文件**

Run: `git status`
Expected: 仅本 plan 涉及的文件被改（`content/episodes/EP001.json`、`src/episode-mainline.js`、`src/app.js`、`src/style.css`、`build.mjs`）。

- [ ] **Step 7.5: （可选）按用户反馈微调**

如用户在 happy-path 走查后反馈「字号太小 / 间距挤 / 颜色不准 / 某分支位置撞 / 等」，根据反馈在 `src/style.css` 或 `src/episode-mainline.js` 微调，然后重新 build + 浏览器验证。每次微调一个属性，commit 消息形如 `polish(mainline): 调段标题字号 12→13px`。

- [ ] **Step 7.6: 最终提交（如有未提交的微调）**

```bash
git add -A
git commit -m "polish(mainline): 最终视觉打磨（基于用户反馈）"
```

---

## Self-Review Notes

**Spec 覆盖核查**：
- ✅ Spec § 2 "核心结构 主线+节点辐射" → Task 2 + Task 3
- ✅ Spec § 4 "数据模型 segments[]" → Task 1
- ✅ Spec § 5.1 "视觉风格" → Task 2.5 + Task 4.5 + Task 6
- ✅ Spec § 5.2 "默认扫读模式" → Task 3（SCAN_LIMIT=2）
- ✅ Spec § 5.2 "焦点模式 + panel" → Task 4
- ✅ Spec § 5.2 "类型筛选" → Task 5
- ✅ Spec § 5.3 "动画" → Task 6
- ✅ Spec § 6.1 文件改动清单 → 全部覆盖
- ✅ Spec § 6.3 "渐进式实施 6 步" → Task 1-6
- ✅ Spec § 7 "验收标准" → Task 7

**未覆盖 / 已知差异**：
- Spec § 6.2 "极坐标布局 上半圆放概念/模型，下半圆放人物/跨期/边界" — 计划简化为 `branchUp` (concepts/models) vs `branchDown`（其他）。功能等价，描述更直接。
- Spec § 4.2 字段名采用 `keyQuote / startTs / endTs / isCore` 驼峰式（项目其他字段也是驼峰，如 `publishedAt / relatedEpisodes`），不是 spec 里的 snake_case，这是与项目惯例对齐的调整。

**Placeholder scan**：无 TBD/TODO/"待定"。Step 4.3 有一个 `// TODO Task 4` 注释在 Task 2.1 的代码里——这个 TODO 是 Task 2 阶段的占位提醒，Task 4 会替换整段，符合渐进式预期，不算 plan 失败。

**类型一致性**：`renderEpisodeMainline(episode, site)` 和 `attachEpisodeMainlineHandlers(rootEl, episode, site)` 签名在 Task 2-4 中保持一致；`segment.id / branches.{type}` 字段名一以贯之。
