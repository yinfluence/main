---

authored_by: external
content_role: reference
trust_level: primary
authoring_mode: human
last_verified: 2026-05-17
---
# 颖响力知识库 SOP

## 目标

这套 SOP 的目标不是“单篇写一篇文稿”，而是把每一期节目拆进一个可持续扩展的网页知识库里。最终产物包含两层：

- 内容层：节目、概念、思想模型、人物、主题、关键词
- 展示层：本地静态网页，`localhost` 可打开

你以后打开一个新终端，只需要传入 `.srt` 路径，就能先把新节目自动收录到这套知识库结构中，再继续补内容。

## 目录结构

知识库根目录：

`/Users/ziqiguo/Documents/Diary/GoldenVault/md/topic/personal/sources/people/颖响力/网页`

关键目录说明：

- `content/episodes/`
  放每一集节目的结构化 JSON
- `content/concepts/`
  放概念卡片。概念比思想模型小，用来概括一个现象
- `content/models/`
  放思想模型卡片。优先引用 GV 现有模型名
- `content/people/`
  放人物源数据节点。用于积累人物背景、别名和节目锚点，前台展示统一并入关键词入口
- `content/themes/`
  放主题节点
- `content/keywords/`
  放关键词节点，作用类似 GV 里的公司/实体入口，也承接所有人物的前台访问入口
- `workbench/`
  放自动生成的 transcript 和 intake 草稿，供整理时使用
- `src/`
  网页前端源码
- `dist/`
  构建后的静态网页

## 已下架功能

### 模拟器测试

- 左侧导航里的“模拟器测试”入口已经下架，不再作为当前网站功能维护。
- `src/simulators/real-estate-tycoon/`、`docs/simulators/real-estate-tycoon/` 等历史文件先保留，不因下架而删除，后续如需恢复必须先重新确认产品方向。
- 常规构建不再刷新模拟器发布副本；做节目、关键词、概念、模型、主题等知识库更新时，不要把模拟器测试作为 GitHub Pages 更新内容一起维护。

上游原始字幕目录：

`/Users/ziqiguo/Documents/Diary/GoldenVault/md/topic/personal/sources/people/颖响力/bilibili/raw`

## 内容设计原则

### 0. 全站语言：简体中文

整个知识库只用**简体中文**写作，覆盖 `content/`、`docs/data/` 和源 JSON 的所有正文、标题、字段值、tags、标签、摘要、观点、延展、AI 草稿、转写稿、字幕导入。

- 禁止繁体字混入：`夢 / 堅 / 邏輯 / 辯證 / 個 / 們 / 發 / 應 / 該 / 學 / 實 / 產 / 國 / 軍 / 經濟 / 體驗 / 業務 / 變化 / 議論 / 難 / 與 / 讀 / 寫 / 標準 / 質量 / 資訊 / 關於 / 樣式 / 風雲 / 愛 / 親 / 優憂 / 麗 / 龍鳳 / 萬億 / 舉 / 舊 / 聲 / 鬧 / 麼 / 點 / 觀` 等。
- 用户提供的原始口播稿、转写稿、嘉宾原话如果出现繁体，必须在入库前全文转成简体；引号内的"原话保留"不构成例外。
- 港台地名、人名、专有名词如果有公认简体写法用简体；只有原文是港台繁体出版物书名/媒体名时可保留繁体并加引号说明。
- 入库自检：写完任一 JSON 字段前，跑一遍繁体扫描（grep 关键繁体字 / 看一眼），发现混入立即转简体再保存。
- 网页前端不做繁简自动转换，写错就是写错，不靠运行时遮盖。

### 1. 节目是入口，不是终点

每个节目 JSON 负责承接当期内容，但真正长期可复用的是：

- 概念
- 思想模型
- 人物
- 主题
- 关键词

节目要不断把内容往这些稳定节点上沉淀。

### 2. 信息、概念、思想模型是点线面关系

- 信息：字幕里的具体材料、表达、细节
- 概念：比思想模型小，概括一个现象
- 思想模型：多个概念按机制或因果关系组织成更高层框架

### 3. 思想模型优先复用 GV 现有模型

如果 GV 已经有贴合模型，优先复用模型名，再结合本期解释。
只有当 GV 现有模型明显不贴合时，才新增模型。

## 分篇索引（按任务读对应子文件）

本 SOP 已按主题拆分。主文只保留目标、目录结构和核心设计铁律；具体执行细则在 `sop/` 子文件里，按当前任务读对应那一篇即可，不必通读全部。

- 新增/整理关键词、判定 `kind`、关键词类型规格、批量执行制度、台账、低质量写法黑名单 → `sop/01-关键词治理.md`
- 视频链接维护、B 站普通/会员/下架判别、`sync:videos`、`video-link-overrides.json` → `sop/02-视频链接维护.md`
- 概念 / 模型 / 主题节点怎么建、双向引用同步规则 → `sop/03a-双向引用与节点生成.md`
- 节点层反向回填顺序、各节点最小字段清单 → `sop/03b-节点回填与字段清单.md`
- 页面导航、返回 / hover、关键词搜索、关键词页怎么写给读者看 → `sop/04a-导航交互与关键词页呈现.md`
- 视觉布局、字体、首页 / 移动端、知识图谱与详情页交互 → `sop/04b-视觉布局与知识图谱.md`
- 节目各字段怎么写、六类内容、从 transcript 提炼主线、写作规范 → `sop/05a-节目字段与写作规范.md`
- 源文件标题标准、`.srt` / `.md` 导入、推荐整理流程、命名规则、工作顺序 → `sop/05b-节目导入流程与命名.md`
- 常用命令、GitHub 发布上线标准、验收标准、当前状态约定、常见坑与防错清单 → `sop/06-发布命令与验收.md`

拆分背景与流程见 `~/.claude/protocols/long-file-splitting.md`。本次拆分日期 2026-06-04，原文件备份为 `SOP-知识库整理.md.bak.20260604`。
