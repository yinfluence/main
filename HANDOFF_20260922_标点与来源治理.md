---
title: 标点与来源治理待办
created: 2026-09-22
status: 待开工
---

# 这份文件管什么

2026-09-22 做完全站化名清理和来源验证之后，剩下三件没做的。当时 token 快用完，
用户决定分次做。下次说到颖响力网站要修东西，先读这份。

前一轮做了什么见 commit `17fb600`，详细过程在 `workbench/audit-20260922/REPORT.md`
（那个目录不入库，只在本机）。

# 三件事，按建议顺序

## 一、关键词之间的断链　已完成（2026-09-22）

31 处清零。三处是 slug 写错，`wu-ren-ji` 和 `zibenguan-zhi` 改成了无人机和资本管制。
其余 28 处删掉了坏链接。

删掉的里面只有两个该补成词条，房票和银行直供房，已经补了。

### 这里踩过一个坑，记下来

第一次判断该不该建词条时，我用的是这个词在 `docs/data/site.json` 全文里出现的次数，
按次数排了个十个词的清单。排序本身没问题，问题是我拿它直接当了结论。
全文计数把 tags、relatedKeywords 和 extensions 里的跨期回指全算进去，数字会虚高。

按次数看，格力 34 次、鲁迅 26 次、安倍 10 次，都像该建。逐个翻上下文才发现：
格力两处都是类比（「改革跟当年格力一样是断臂求生」「没法跟美的、格力较劲」），
鲁迅三处都是举例（「像当年鲁迅的小说」「语文课上没人真的关心鲁迅」），
安倍那一次是政策名的一部分（「靠的是安倍那三支箭」），伯克希尔一期都没出现过。
北部湾港那两次是行程地点，节目没讲这个港口本身。

最容易看走眼的是阿塞拜疆。EP097 里出现五次，看着是强关联，实际讲的是
伊朗国内的阿塞拜疆族群，跟阿塞拜疆这个国家不是一回事，原来挂在北高加索下面
本身就挂错了。

**正确的判法**：先 grep 出命中的期，再逐个看它在那一期是被当成材料讲，
还是只在 extensions 的跨期回指里被提一句。sop/01 写得很清楚，
弱相关只能写进自然语言延展，不进相关节目区。词频只能用来排查看顺序，
不能直接当判据。

## 二、弱百科单来源（50 个词条）

按 `sop/01-关键词治理.md` 的门槛，弱百科不能作为唯一来源。

优先级按 kind 分：

人物 9 个和机构 8 个最该补，里面有现任政要和公司状态这类高风险事实。
地理 8 个次之，人口和经济数据会变。
概念 7 个、事件 7 个、通用 5 个、机制 3 个里，历史事件和神话人物风险低，
维基其实够用，可以只补有争议的。

跑这个拿清单：

```bash
python3 - <<'PY'
import json,io,glob,re
W=re.compile(r'(wikipedia\.org|baike\.baidu\.com|zhihu\.com|douban\.com)')
for p in glob.glob('content/*/*.json'):
    d=json.load(io.open(p,encoding='utf-8'))
    u=[s.get('url','') for s in (d.get('sources') or []) if s.get('url')]
    if u and all(W.search(x) for x in u):
        print(d.get('kind'),d.get('name'),p)
PY
```

做法照 2026-09-22 那轮：一个助手管五到六个词条，要求它每条链接自己 curl 验证
返回 200 并在页面里 grep 到对应原文，主线程再抽验一遍。上一轮五个词条一个助手
大约烧 15 到 17 万 token，50 个词条算下来 150 万上下。

**这一轮踩过的坑，别重复：**

政府和机构站点反爬的不少。Britannica、Forbes、IMF、RBA、ScienceDirect、
Statista 对 curl 一律 403 或 000，得换 WebFetch，有的连 WebFetch 也进不去。
`ir.nio.com`、`ir.lixiang.com`、`ir.mi.com`、`en.kremlin.ru`、`gsxt.gov.cn`
这几个从本机根本连不上，要换来源。`npc.gov.cn` 只有 http 能开，https 握手失败。
`costco.com.cn` 和 `xibei.com.cn` 全站是前端空壳，任何路径都返回同一份容器 HTML。

搜索结果页绝对不能当来源，上一轮清掉过两条百度搜索页。网站首页顶替具体页面
也不行，清掉过八条。

## 三、全库标点（最大，最后做）

393 个文件命中，四类问题：

| 类型 | 数量 | 能不能脚本批量 |
| --- | --- | --- |
| 破折号 `——` | 799 | 不能，要逐处改写 |
| 中文之间的半角逗号 | 844 | 能 |
| 英文直引号 | 402 | 能，但要成对转 |
| 中文之间的半角冒号 | 122 | 能 |

破折号是硬骨头。用户 2026-07-29 的全局禁令，改法是把句子重组，光删符号不算改。
多数是先陈述再补充说明的结构，补充部分如果是同位语（后面跟着是、指、即、一个）
改成逗号，其余断成两句。改完必须回头念，上一轮在 `fuwei-biaoju.json` 上
20 处里有一处断完不通顺，是人工念出来的，脚本没抓到。

半角标点和引号可以用这段批量转，上一轮在九个化名词条上验证过：

```python
def fix(t):
    out=[];q=False
    for ch in t:
        if ch=='"': out.append('“' if not q else '”'); q=not q
        else: out.append(ch)
    t=''.join(out)
    t=re.sub(r'(?<=[一-鿿”》）]),(?=[一-鿿“《（0-9])','，',t)
    t=re.sub(r'(?<=[一-鿿”》）]);','；',t)
    t=re.sub(r'(?<=[一-鿿”》）]):(?=[一-鿿“《（0-9])','：',t)
    return t
```

转完要数一遍左右引号是不是配平，不配平说明原文引号本来就不成对，得人工看。
批量替换只走 JSON 解析后的字符串值，别直接在文件文本上改，会动到 JSON 结构本身。

估 80 到 120 万 token。建议按目录分批，`content/keywords` 一批，
`content/people` 一批，`content/concepts` 和 `models` 一批，
`scripts/keyword-definitions.d` 一批。

# 每一轮收口都要做的

```bash
npm run build
python3 scripts/audit-episodes.py
node scripts/audit-lives.mjs
node scripts/audit-keywords.mjs
python3 scripts/check-alias.py
python3 ~/.claude/scripts/ai-flavor-check.py <改过的文件>
```

`check-alias.py` 是 2026-09-22 新写的，查标签和正文里的化名残留。
发现新的误报类型当场补进脚本里的两张排除表，别只改那一处文字。

提交前确认 `git status` 里没有把 `bilibili/live/*.srt` 这些用户自己的未跟踪文件
一起带进去。
