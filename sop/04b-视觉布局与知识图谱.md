---
title: 视觉布局与知识图谱
part_of: SOP-知识库整理.md
---

### 固定图标与入口规则

- 全站右下角固定保留两枚按钮：
  - 回首页
  - 返回顶部
- 两个按钮桌面端和手机端都固定在右下角
- 返回顶部按钮默认常驻显示，不需要滚动后才出现
- 首页标题下面固定显示平台主页入口：
  - `bilibili`
  - `YouTube`
- 平台主页入口放在标题正下方，不放在标题右边
- 手机端左上角三明治按钮默认使用更浅、更透明的样式，减少对正文遮挡
- 侧边栏打开时，三明治按钮再恢复为深色高对比状态
- 节目页标题下方的视频入口也是固定规范：
  - 普通 B 站：蓝色入口
  - 会员 B 站：金色入口
  - 已下架 B 站：灰色入口，hover 显示 `已下架`
  - YouTube：固定为 YouTube 风格入口
  - 平台入口必须有 hover 反馈
- 节目页顺序固定为：
    1. 标题
    2. 摘要
    3. 标签
    4. 视频平台入口

### 字体与职业感规则

- 整站字体要优先体现知识库/文档站的职业感，而不是泛博客感或系统默认感
- 正文优先使用稳定、专业的中文无衬线栈
  - 优先 `Noto Sans CJK SC / Source Han Sans SC`
  - 再回退到 `PingFang SC / Hiragino Sans GB / Microsoft YaHei`
- 标题优先使用更克制的中文衬线栈
  - 优先 `Noto Serif CJK SC / Source Han Serif SC`
  - 再回退到 `Songti SC / STSong`
- 正文和标题不能继续沿用偏个人博客感的随意混搭英文字体作为主字体方案
- 标题、正文、说明、标签必须是四层不同语气，不能做成“所有字像一段话”
  - 页面主标题：最大、最紧、最有识别度
  - 模块标题 / 卡片标题：次一级，但仍然明显区别于正文
  - 正文 / 摘要：稳定、退后、可长时间阅读
  - 标签 / 说明 / eyebrow：最小、最轻、最克制
- 标题和正文不只靠字号区分，还要同时拉开：
  - 字体家族
  - 字重
  - 行高
  - 字色
  - 字距
- 卡片标题、列表标题在移动端必须一眼看出是“标题”，不能和下面摘要融成一个段落
- 正文颜色应比标题更退后，避免标题和正文同黑同重
- 说明类文字、section note、inline label、eyebrow 必须统一走更小、更浅、更高字距的辅助层，不要和正文抢注意力

### 滚动与对齐规则

- 网页端和手机端都优先保证自然、轻量的滚动手感，不做重度 scrolljacking
- 手机端长页面滚动应尽量自然落在大块内容起点附近，避免停在半张卡片中间
- 首页 hero、首页 section、详情页 header、详情页 section 应使用一致的轻量滚动对齐策略
- 如果使用滚动对齐，优先使用 proximity，而不是强制 snap
- 每次改动滚动体验，都必须本地验证桌面端和手机端截图，不只看代码
- 自动辅助滚动只在用户已经接近模块边界时轻量介入，目标是把页面送到最接近的模块起点，而不是强拉到固定位置
- 如果上下两个模块同时卡在屏幕中间、两边都露出半截内容，应自动选择更合理的落点，避免页面停在“两边都不完整”的中间态
- 自动辅助滚动必须结合当前滚动方向、可见面积和距离一起判断，不能只靠单一阈值
- 文档底部或用户明显还在主动拖动时，禁止反向拉回
- 快速滚动时允许出现极轻的短时模糊和章节轮盘提示，但轻微滚动不应明显模糊
- 模糊的退场必须早于轮盘退场，不能让页面在用户已经停下后还长时间发糊
- 悬浮搜索栏在用户上滑时可以回显，但如果用户没有继续交互，应在短暂停留后自动收起，避免长期遮挡正文
- 知识详情页（概念 / 模型 / 主题 / 关键词 / 人物）首屏不应再跑额外 reveal 入场动画
  - 页面路由完成后，标题区与首屏正文必须一次性稳定落位
  - 禁止出现“页面已经到了，但标题区又闪一下 / 跳一下”的二段动画感
- 知识详情页之间仍然应保留极轻的切换过渡
  - 目标是“有一点网页切换感”，不是“完全没动画”
  - 只允许短时淡入淡出与极小位移
  - 禁止恢复重 blur、重缩放、长距离位移、翻页感过强的动画

### 首页交互边界

- 首页 `节目索引` 的左右按钮、滑动手势、自动轮播必须视为同一套状态机
  - 三者都只能驱动同一个 `currentIndex`
  - 每次只允许移动 `1` 个节目
  - 自动轮播等价于点一次“下一张”
- 首页节目索引的响应式判断必须按内容区真实宽度决定 `3 / 2 / 1`
  - 宽时 `3` 张
  - 中间宽度 `2` 张
  - 更窄 `1` 张
  - 任何时候都不允许出现两排
- 首页节目索引的首屏页数判断必须稳定
  - 首次渲染与完成布局后的最终渲染必须使用同一套宽度基准
  - 不允许出现“首屏先显示 3 张，随后又掉成 2 张”这类跳变
  - 需要优先排查滚动条 gutter、容器真实宽度、首屏 fallback 宽度不一致这三类原因
  - 如果边界宽度容易抖动，应优先稳定宽度来源，而不是让首页反复重判
- 首页节目索引切换时禁止整页重渲染
  - 只能局部更新轮播区域
  - 否则容易把搜索栏、滚动位置、焦点状态一起打乱
- 首页节目卡片高度必须收敛到稳定区间
  - 标题和摘要允许裁切，但卡片外框高度要稳定
  - 不能因为某一期标题更长、摘要更满，就把 `概念入口` 和后续区块整体往下顶
- 首页节目卡片必须按“标题 / 简介 / 关键词”三段式稳定分配高度
  - 标题允许比普通卡片更长，但必须有固定最大行数；超出时用 `...`
  - 简介也必须有固定最大行数；内容短时完整显示，超出时再截断
  - 简介截断必须保留 `display: -webkit-box`、`overflow: hidden` 和固定 `-webkit-line-clamp`，不允许被后续更高优先级样式改回完整展开
  - 关键词区必须固定在卡片底部，不能被长标题或长简介挤出边框
  - 如果某一期标题特别长，优先裁掉标题和简介的尾部，不要牺牲关键词区可见性
  - 首页节目卡片最多渲染前 `8` 个关键词；不要用固定高度、边框或 `overflow` 裁切来伪装减少标签
  - 首页卡片默认只展示可控数量的关键词，不要为了“全展示”把底部区域撑坏
- 新节目状态必须自动计算，不靠手工改字
  - 以节目原始源文件时间为准
  - 最近 `3` 天内：
    - 首页节目卡片把 `已整理` 替换成红色 `新`
    - 节目索引卡片里的节目编号旁边也显示红色 `新`
    - 内容页标题后面显示一个小号红色 `新`
  - 超过 `3` 天后：
    - 首页自动恢复成 `已整理`
    - 节目索引卡片里的 `新` 自动消失
    - 内容页标题后的 `新` 自动消失
  - 不允许依赖“过几天手工再改一次”
- 首页节目卡片的状态文案只允许使用：
  - `新`
  - `已整理`
  - `待整理`
- 会员状态不允许替代首页节目卡片的整理状态
  - 会员信息只在视频入口层表达
  - 首页卡片不要显示 `会员` 去覆盖 `已整理` 或 `新`
- 但“稳定”不能以牺牲主标题可读性为代价
  - 手机端节目标题优先保证完整显示
  - 如果必须压缩空间，先降字号、调行高、减少摘要行数，而不是先裁掉标题
- 自动轮播、按钮切换、手势切换都不得改写当前滚动位置
  - 不能把页面突然拉回搜索区
  - 不能触发章节吸附把页面带偏
- 手机端节目索引不允许只做“松手后换页”的硬切
  - 必须支持真实左右拖动
  - 拖动过程中要能看到前一张 / 后一张的边缘预览
  - 视觉上更接近翻页，而不是整块闪切
- 但如果手机端节目索引的主卡被左右按钮和边缘预览压得过小，应优先重做交互模式，而不是硬保留桌面版结构
  - 手机端允许取消左右箭头
  - 手机端允许取消两侧边缘预览
  - 目标是保证主卡正文面积、标题可读性和点击成功率
- 手机端首页节目索引优先采用“单卡全宽 + 左右滑动切换”的工作模式
  - 主卡必须尽量吃满可用宽度
  - 左右滑动负责切换节目
  - 点按负责跳转，不允许和滑动职责混在一起
- 手机端首页节目索引的滑动手感必须连续
  - 达到翻页阈值后，不允许先往回弹一下再翻过去
  - 视觉上要像顺着手势继续完成翻页，而不是“回抽一下再补动画”
  - 如果手感发涩，优先调横向识别阈值、位移预览幅度和翻页触发阈值
- 手机端首页节目索引必须提供明确、稳定的打开入口
  - 点整张卡可以打开
  - 点标题可以打开
  - 如果交互上仍可能误触，允许额外提供单独的 `打开节目` 按钮
  - 不允许出现“点了好几次都没反应”
- 手机端首页节目索引的自动轮播可以保留，但频率要更慢
  - 默认按 `10 秒` 一次处理
  - 不能频繁打断阅读
- 用户正在拖动或点节目索引按钮时，自动轮播必须暂停
  - 不能一边用户在操作，一边系统自己换页
  - 自动轮播只在用户没有继续干预时恢复
- 手机端首页节目索引的摘要不能继续直接塞原始长摘要
  - 需要单独生成一版移动端摘要
  - 目标长度接近原摘要的 `75%`
  - 优先按句子边界或停顿边界收尾
  - 禁止出现“半句被硬截断悬在那里”
  - 但也禁止压缩成过短口号式文案
- 首页搜索输入时，搜索栏和结果区必须一起进入可见区
  - 不能只把输入框顶到上面而让结果区留在原位置
  - 搜索栏与结果区之间保留层次，不做成一整块黏连
- 首页搜索栏的显隐边界固定为：
  - 回到搜索区原始位置时稳定可见
  - 离开搜索区后，上滑可短暂回显
  - 停住不继续交互时自动收起
  - 节目轮播切换不得影响它的显隐状态
- 右侧章节轮盘不应在首页首屏直接出现
  - 进入后续内容区后再出现
  - 轮盘提示退场应晚于轻微滚动停止，但不能长期停在屏幕上
- 右侧三按钮展开后允许短暂保留，但无操作时必须自动收回
  - 收回后只保留最小入口
  - 不得打断正在点击的按钮
- 左侧导航最终结构固定为：
  - 主导航：`首页 / 节目 / 概念 / 模型 / 人物 / 主题 / 关键词`
  - 搜索区只保留 `搜索知识库`
  - 底部单独保留 `知识图谱`
  - `网页日志` 固定在 `知识图谱` 下方
  - 不再恢复旧的重复折叠块结构
- 首页不使用过度保守的延迟显现策略
  - 首页模块在初次渲染后应尽快全部可见
  - 不允许用户已经滚到页面中段，`概念入口 / 思想模型 / 知识图谱` 仍然大面积留白
- 首页视觉层级不允许长期停留在“整页同一层浅色”
  - 至少通过分区底色、边框强弱、文字层级三种手段把不同模块分开
  - 默认态就要能分出主区块，不要把“只有 hover 才有层次”当成常态
  - 但分区底色必须保持低饱和、统一气质，禁止做成花花绿绿的廉价拼色
  - 分区之间只能做轻微温度差和明度差，不要做强烈彩色块
- 知识详情页里的 `相关节目` 证据层必须明确区分“节目卡本体”和“关键词入口”
  - 桌面端默认态：节目卡浅底深字，关键词 chip 深底浅字
  - 桌面端 `hover / focus`：节目卡切深底浅字，关键词 chip 反转成浅底深字
  - 移动端可按空间与可读性反转主次，但仍必须保持“节目卡”和“关键词 chip”一眼可区分
  - 节目标题默认不使用多余下划线装饰，优先靠颜色、层级和 hover 反馈表达主入口
  - 不允许出现“节目卡和关键词都是同一明暗逻辑，导致主入口和次入口混在一起”的状态
  - 如果某条规则导致浅底浅字、深底深字或 hover 后反差错误，必须以可读性为准改掉旧规则
- 正文里的 `EPxxx` 内联说明浮窗必须是全局浮层，不得受 accordion、卡片或 section 容器裁切
  - 浮窗优先保证留在视口内
  - 右下放不下就换右上，再不行换左侧，不允许固定朝一个方向溢出
  - 浮窗逻辑服务正文分析区，不要反过来干扰证据层节目卡
- 知识详情页右侧章节轮盘在 `相关节目` 区内，必须支持 EP 级别分项
  - 不能只把整块 `相关节目` 当成一个笼统 section
  - 至少要能看到 `EP122`、`EP090` 这类条目并支持直达

### 首页移动端节目索引硬规则

- 手机端首页 `节目索引` 的主卡优先级高于边缘预览、箭头装饰和花哨轮播感
- 如果空间冲突，应按这个顺序删减：
  1. 左右箭头
  2. 边缘预览
  3. 多余装饰
  4. 最后才考虑压缩标题和摘要
- 手机端首页节目索引标题必须明显大于正文，且一眼可读
- 手机端首页节目索引摘要允许比桌面短，但必须像正常摘要，而不是像残句
- 手机端首页节目索引点按测试必须单独验证：
  - 轻点整卡能打开
  - 轻点标题能打开
  - 轻点按钮能打开
  - 左右滑动切换时不会误开
- 首页头像必须完整显示
  - 不允许出现“头像只露出顶部一条”或被默认裁切规则截掉的状态
  - 如果头像源图不是刚好适配圆形容器，优先调整显示方式让头像完整可见，而不是强行 `cover` 裁掉脸部

### 自动化 UI 回归硬规则

- 只要改动涉及首页、节目索引、浮动按钮、导航、滚动交互、节目详情页头部或视频入口，就必须跑自动化 UI 测试
- 自动化 UI 测试必须分别覆盖：
  - 桌面版视口
  - 移动版视口
- 任何一端失败，都不能算完成
- 固定命令：

```bash
npm run test:ui
```

- 当前自动化 UI 回归至少要验证：
  - 桌面首页节目区能正常渲染，且保留左右切换按钮
  - 桌面首页节目区底部存在 `更多节目`
  - 移动首页节目区底部存在 `更多节目`
  - 首页 `更多节目` 默认态就是深色背景 + 白字，不依赖 hover 才变深
  - `EP124` 节目详情页能显示 B 站会员标识
  - 手机端首页在已置顶时点击“向上”按钮，会展开完整四个悬浮按钮
  - 首页悬浮放大镜点下后，必须进入与 `#/episodes` 右侧放大镜一致的搜索态
  - 从首页点 `查看更多` 进入 `#/episodes` 时，必须回到节目索引默认首屏状态
    - 不能继承上一次停留的分组
    - 不能继承上一次残留的搜索词
  - `#/episodes` 默认态必须是“集数区间 + 右侧放大镜”
  - `#/episodes` 顶部集数工具条必须覆盖：
    - 大幅下滑后隐藏
    - 大幅上滑后重新出现
    - 悬浮状态闲置 10 秒后自动隐藏
    - 重新出现后仍可点击并切换区间
  - `#/episodes` 节目卡片头部不显示 `查看` 文案，整卡点击进入节目详情
  - 节目索引里点右侧节目轮盘条目后，必须直接进入对应节目详情页
  - `#/episodes` 搜索态不会因定时器自动收起，必须由用户主动退出
  - 从别的页面调起节目搜索但未进入结果时，退出后必须回到原页面和原滚动位置
  - `#/episodes` 搜索空态会显示 `推荐关键词 + 换一换`
  - `推荐关键词` 点击后，会把词带进搜索框并显示相关节目，而不是直接跳知识页
  - 上方知识建议点击时，人物结果优先进入对应人物关键词页
  - `#/episodes` 底部左右切组按钮在手机端必须达到更大的点击尺寸
- 自动化 UI 截图产物默认写到：
  - `.local/ui-smoke/`
- 自动化 UI 不是人工检查的替代品；脚本通过后，仍然要抽查关键页面是否符合这份 SOP 的版式与交互要求

### 知识图谱与详情页表面规则

- 知识图谱中间那块"节点画布"不允许长期使用纯黑或接近纯黑底
  - 画布必须优先服务节点关系可见性
  - 如果深底导致节点、标签或关系难看，就应回到浅底或浅灰底
- 知识图谱右侧信息卡可以与画布分开处理
  - 画布可浅
  - 侧栏可深
  - 不强求整个图谱页统一成一种明暗方案

### 知识图谱交互规则（焦点-扩散模式）

知识图谱的本质是给读者一条"从一个节点出发看世界"的路径，不是一张全景挂图。默认状态可以是 506 个节点的星云，但**用户一旦点选某个节点，必须进入"焦点-扩散"模式**：

- **焦点节点必须重新居中**：被点选的节点要平滑动画移动到画布中心（或视觉重心），不能停在原位让用户自己找。
- **关联节点必须从焦点向外径向扩散**：用 force-directed 或显式径向 layout 让该节点的一阶邻居从焦点位置动态展开到环形排布，配 200–400ms 的过渡动画。视觉上应该读出来是"这个节点连出了哪些东西"。
- **非关联节点必须隐藏或大幅淡出**：不能像现在这样保留 ~10% 不透明度的一整张彩色背景图。背景节点要么彻底隐到 0%，要么压到 ≤3% 灰度，让焦点子图成为画布上唯一显眼的内容。
- **信息卡内容必须按类型分组**：右侧/右上的信息卡列该节点的关联节点时，按 `节目 / 概念 / 模型 / 主题 / 人物` 分块，不要混排。当前"9 条链接"这种纯数字提示信息密度不够。
- **二次点击邻居要继续扩散，不是替换焦点**：在焦点模式里点一个邻居节点，应该是把焦点平滑切换过去 + 重新扩散，而不是瞬间跳变。形成"沿着关联走"的连续体验。
- **必须有一键回到全景的按钮**：进入焦点-扩散模式后，要有显眼的"返回全图"或"清除焦点"控件。
- **画布默认状态下，未点选时不要把节点排成大密度网格**：当前默认排布像一面"色斑墙"，缺少可读性。默认状态优先按 `节目时间轴` 或 `节点类型分簇` 排布，让用户一眼能看出"这是知识图谱"，不是一片彩色噪点。

实现层面：`src/graph-view.js` 里要拆出 `enterFocusMode(nodeId)` 和 `exitFocusMode()` 两个明确状态，过渡用 D3 transition 或等价 CSS 动画。不允许把"点了节点 → 别的什么都没变"作为可接受的交互状态。


- 详情页 `split` 双栏卡片禁止使用廉价的浅绿 / 浅蓝拼色
  - 如果要分栏区分，只允许使用中性米灰、灰米、灰纸色的轻微变化
  - 原则是“统一、克制、稳定”，不是“彩色卡片配对”

### 节目详情页展开规则

- `话题` 下各项使用折叠结构
- `核心观点` 下各项使用折叠结构
- `延展问题` 不折叠，直接展开
- `关联人物` 不折叠，直接展开
- `关联主题` 不折叠，直接展开
- `关联节目` 不折叠，直接展开

### 关联节目展示规则

- 关联节目不要用横向标签
- 关联节目统一使用纵向列表
- 每条列表至少显示：
  - 节目编号
  - 标题
  - 一行摘要
