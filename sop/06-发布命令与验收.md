---
title: 发布命令与验收
part_of: SOP-知识库整理.md
---

## 验收标准

一条新节目整理完成，至少满足：

1. 首页或节目索引里能点进去
2. 节目页有完整 `summary / topic / viewpoints`
3. 至少链接到一组稳定节点：
   - 概念
   - 思想模型
   - 人物 / 主题 / 关键词
4. 页面能从节目跳到概念，再跳回节目
5. `npm run build` 成功

## 当前状态约定

- `status: "curated"` 表示这期已经做了相对完整的结构化整理
- `status: "draft"` 表示这期已经进入知识库，但仍然是自动生成的草稿，需要继续人工补内容
- 首页统计中的：
  - `curatedEpisodes` = 已精修节目数
  - `draftEpisodes` = 已导入但待整理节目数

## 常用命令

```bash
cd "/Users/ziqiguo/Documents/Diary/GoldenVault/md/topic/personal/sources/people/颖响力/网页"
```

导入字幕：

```bash
npm run srt -- "/path/to/[EP123】 标题.srt"
```

强制覆盖草稿：

```bash
npm run srt -- --force "/path/to/[EP123】 标题.srt"
```

批量补齐所有缺失节目草稿：

```bash
npm run bootstrap:all
```

重建网页：

```bash
npm run build
```

启动本地网页：

```bash
npm run serve
```

开发时一步到位：

```bash
npm run dev
```

## GitHub 与上线发布标准

这个项目现在不是“本地改完就算上线”，而是 GitHub 驱动 Cloudflare 部署。

### 当前发布链路

- GitHub 仓库：`yinfluence/main`
- 生产分支：`main`
- Cloudflare Workers Builds 读取仓库里的 `docs/`

### 推送到哪个 remote（反复踩坑，必须先看）

本地配了 3 个 remote，只有一个是对的。推错会 403，线上不更新，人却以为推成功了：

- `origin` → `ziqiguo02-prog/main`：**错的**。当前 Git 凭据是 yinfluence 账号，对这个仓库没有写权限，`git push origin` 会报 `403 / Permission denied`。
- `yinfluence-origin`（和 `yinfluence`）→ `yinfluence/main`：**对的**，就是部署 yinfluence.org 的仓库。本地 `main` 的上游也是 `yinfluence-origin/main`。

唯一正确的推送命令：

```bash
git push yinfluence-origin main
```

硬规则：

- 永远显式写 `git push yinfluence-origin main`。不要用裸 `git push`，也不要 `git push origin`——裸 push 走哪个 remote 取决于配置，别赌。
- 推送前先核一眼：`git branch -vv`，确认当前 `main` 的上游是 `yinfluence-origin/main`（不是 origin）。
- 推送后看输出里有 `-> main` 且无 403，再去 yinfluence.org 强刷验证。若看到 `Permission to ziqiguo02-prog/main.git denied`，就是推错 remote 了，改用 `yinfluence-origin`。
- `git add` 要显式列本任务文件，不要 `git add -A`——仓库里常有用户既有改动（如 `docs/simulators/` 的 README）和 `.omx/`、`.superpowers/` 等无关目录，全 add 会把它们一起提交。

所以发布的关键不是只改本地文件，而是：

1. 本地把内容和页面改好
2. 如果这次准备上传 GitHub，先同步更新 `网页日志`
3. 执行 `npm run build`
4. 确认 `docs/` 已同步更新
5. `git add`
6. `git commit`
7. `git push` 到 `yinfluence/main`

如果不 `commit + push`，GitHub 不会更新，Cloudflare 也不会更新。

### 发布前最低动作

```bash
npm run build
git add <本次任务涉及的具体文件>   # 不要 git add -A，避免带上用户既有改动与无关目录
git commit -m "<符合 Lore 协议的提交信息>"
git push yinfluence-origin main      # 只推这个 remote，不要 origin、不要裸 push
```

发布前额外硬规则：

- 只要这次要上传 GitHub，就必须先更新 `网页日志`
- `网页日志` 不需要写成长文，但至少要按时间列出这次改了哪些页面、交互或结构
- 不允许出现“代码已经 push 了，但网页日志还停在旧状态”

### 发布前强制自检（先自检合规，再上传）

铁律：整理完成 ≠ 可以上传。`push` 之前必须先把本期对照 SOP 逐项自检，全过才能 commit + push。真实事故：EP155 首轮没自检就 push，事后才发现漏了三项——关键词词条没建、网页日志没更新、`topic.extensions` 小标题用了抽象黑话。把下面 6 项固化成 push 前关卡，不靠记忆。任一不过，先修再 build，不许先 push。

1. **字数自检**：跑 `sop/05b` 的字数脚本，全部落区间——`summary` 200–250、`mechanism` 150–250/5–6 步、`topic.extensions` 每条 80–130、根 `extensions` 30–80、`viewpoints` 4–6 条且 body 80–150。
2. **关键词词条数**：`grep -l EPxxx content/keywords/*.json | wc -l` 必须 ≥ 本期 `tags` 数。已存在的词条回填 `episodes`，缺的新建（写满该 kind 字段，不留 stub）。
3. **双向回填**：`concepts / models / themes` 每个被引用节点的 `episodes` 数组都含本期；分集系列上下集 `relatedEpisodes` 互引。
4. **延展小标题**：`topic.extensions` 一律“关于 xxx”，且 xxx 是普通读者一眼能懂的人/事/国/行业；禁止抽象黑话（如“区域拒止”“大国竞争的时间结构”“守墙合同与成本转嫁”）。
5. **网页日志**：`src/app.js` 的 `WEBSITE_LOG_ENTRIES` 已加本期条目；不允许代码已改、日志还停在旧状态。
6. **发布时间**：`publishedAt` = 平台真实发布时间（B 站 `pubdate` 换算），不是整理日期、不是未来日期。

### 发布前必须检查

1. `网页日志` 已覆盖本次上传涉及的主要改动
2. `npm run build` 成功
3. `npm run test:ui` 成功，且桌面版与移动版都通过
4. `docs/index.html` 与当前构建产物一致
5. 本地关键页面已验证
6. GitHub 远端 `main` 已更新到最新 commit

### 如果 GitHub 更新了，但网站没更新

优先检查：

1. Cloudflare 有没有拉到新的 commit
2. Cloudflare Build 是否真的部署 `docs/`
3. 是否需要清缓存
4. 线上返回的 `index.html` 是否真的已经换成最新结构

硬检查方法：

1. 先核对 GitHub 远端 `main` 的最新 commit
2. 再直接请求线上首页 HTML
3. 看线上 HTML 里是否已经出现本次改动的关键标记

例如如果本次改动包含：

- `brand-lockup`
- `brand-avatar-link`
- `desktop-menu-button`

而线上 HTML 仍然没有这些标记，就说明：

- 不是 CSS 没生效
- 不是图片没加载
- 而是 Cloudflare 生产环境还在 serving 旧 deployment / 旧构建

也就是说：

- GitHub 新了，不代表线上一定新了
- 必须以线上实际返回的 HTML 为准，而不是只看仓库

### Cloudflare 本地直推的阻塞说明

如果要用 `wrangler deploy` 直接发布，当前环境必须满足以下任一条件：

- 本机已经 `wrangler login`
- 或已经设置 `CLOUDFLARE_API_TOKEN`

否则会出现：

- `You are not authenticated`
- 或 `set a CLOUDFLARE_API_TOKEN`

这类错误

一旦出现这类错误，说明当前终端没有直接控制 Cloudflare 的权限。
这时能做的事情是：

1. 先确保 GitHub 上的 `docs/` 已经是最新
2. 再去 Cloudflare 后台确认：
   - 绑定仓库是不是 `yinfluence/main`
   - production deployment 对应的 commit 是不是最新
   - output directory 是不是 `docs`

也就是说：

- GitHub 没更新：先 `push`
- GitHub 更新了但线上没变：查 Cloudflare deployment / cache

### 新终端执行原则

新终端如果完成了站点改动，默认不要停在“本地已经好了”。

必须继续做完：

1. `npm run build`
2. `git commit`
3. `git push yinfluence-origin main`

只有远端更新，才算真正进入部署链路。

## 常见坑与规避（AI 执行防错清单）

这一节记录实际整理中反复发生的执行坑，照做可绕过，不要再踩。

### 1. 不要并行炸一堆 bash 探查目录

- 坑：一次性发十几条并行 bash 探查目录，输出会乱序拼接、互相 cancel，极易误判成"项目损坏 / 有重复目录"。本项目结构完整，先怀疑自己的操作，不要先怀疑环境。
- 规避：探查用串行、小批；一条命令尽量自带清晰分隔标记（`echo "=== X ==="`）。结果存疑就单条复跑一次确认。

### 2. Read 和 Edit 不要放进同一并行批次

- 坑：把"读文件"和"改同一文件"放进同一批工具调用，Edit 会因为文件还没真正读到而报 `File has not been read yet`。
- 规避：先 Read 完成，下一批再 Edit。

### 3. Edit 的 old_string 不要按"文件结尾"猜

- 坑：很多节点 JSON 的 `episodes` 数组在文件中间，后面还有 `signals / boundaries` 等字段。用"数组 + `]` + `}`"当 old_string 会匹配不到。
- 规避：用数组里最后一个 `note` 的真实唯一文本当锚点，在它后面插入新条目，不要用文件尾部结构去猜。

### 4. node 校验 JSON 用绝对路径或确认 cwd

- 坑：`node -e "require('./content/...')"` 在 cwd 不对或文件名不存在时直接抛栈、污染输出。
- 规避：校验合法性统一用 `node -e "JSON.parse(require('fs').readFileSync(process.argv[1],'utf8'))" <abs-path>`，或先确认 `pwd` 在 `网页/`。

### 5. 不要用 git stash 试 baseline

- 坑：`git stash push -- <file>` 带上不存在的文件会报错，且 `stash pop` 可能提示 entry kept、让人误以为改动丢了。
- 规避：要确认某个失败是不是自己引入，用 `git stash` 之外的方式——直接看该失败是否引用了本次文件；或 `git diff --stat` 看影响面。本项目里 `npm run test:ui` 的关键词分组失败是 pre-existing，与新增节目无关，不要反复用 stash 去验证。

### 6. 数量基准（避免少写）

- 关键词 `tags`：纯口播分析期一般 8-12 个，叙事/人物/事件密集期可到 15-20；硬规则仍是 `tags 数 = 词条数`，一个不漏。
- `concepts`：一般 3-6 个；`models`：一般 2-4 个。优先复用既有节点（如组织失声、委托代理、制度形式主义螺旋、筛选机器），不够再新建。
- 新建词条必须手写满该 kind 的字段，绝不留 build 自动 stub。
